using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KnaveHero : BaseHero {

	// Use this for initialization
	void Start () {
        this.ManualAssignStats (2, 2, 2, 25, 200, 1);
    }

    // Update is called once per frame
    void Update () {
		
	}
}
